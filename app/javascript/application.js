import "jquery"  // This will load jQuery
import "popper"  // Popper.js
import "bootstrap"  // Bootstrap JS (which depends on jQuery)
import "@hotwired/turbo-rails"  // Turbo functionality
import "controllers"  // Stimulus controllers
