<%= form_with model: @enrollment, url: course_lessons_path(@course) do |form| %>

    <div class="form-group">
    <%= form.hidden_field :user_id, value: current_user.id  %>
    </div>

    <div>
    <%= form.button "Enroll confirm", type: "submit" %>
    </div>
<% end %>
