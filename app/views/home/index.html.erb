<% if current_user && (current_user.admin? || current_user.instructor?) %>
  <div class="text-center mt-4">
    <%= link_to 'Create Course', new_course_path, class: 'btn btn-success btn-sm rounded-pill px-4 py-2 text-decoration-none' %>
  </div>  
<% end %>
